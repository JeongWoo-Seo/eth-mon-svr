syntax = "proto3";

package gas_prediction;

option go_package  = "github.com/JeongWoo-Seo/eth-mon-svr/pb";

import "gas_prediction.proto";

service GasPredictionService {
    rpc sendGasPrediction(GasPredictionRequest)returns(GasPredictionResponse);
}